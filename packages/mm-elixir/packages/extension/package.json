{
  "name": "@pi-elixir/extension-internal",
  "version": "0.8.4",
  "private": true,
  "description": "Internal TypeScript extension workspace for pi-elixir",
  "license": "MIT",
  "type": "module",
  "imports": {
    "#src/*": "./src/*"
  },
  "scripts": {
    "lint": "pnpm run lint:guard && pnpm run lint:structure && oxlint -c .oxlintrc.json --type-aware --type-check src/",
    "lint:guard": "node scripts/check-oxlint-rules.mjs",
    "lint:structure": "oxlint -c .oxlintrc.json src/ test/ vitest.config.ts vitest.integration.config.ts",
    "fmt": "oxfmt --write .oxfmtrc.json .oxlintrc.json package.json tsconfig.json vitest.config.ts vitest.integration.config.ts src/ test/",
    "fmt:check": "oxfmt --check .oxfmtrc.json .oxlintrc.json package.json tsconfig.json vitest.config.ts vitest.integration.config.ts src/ test/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:integration:smoke": "vitest run --config vitest.integration.config.ts test/integration/extension-stdio-smoke.test.ts",
    "test:dupes": "jscpd src --min-lines 5 --min-tokens 50 --format typescript --threshold 0",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run fmt:check && pnpm run test && pnpm run test:dupes"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "dedent": "^1.7.2",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "jscpd": "^4.0.5",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "oxlint-tsgolint": "^0.23.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
