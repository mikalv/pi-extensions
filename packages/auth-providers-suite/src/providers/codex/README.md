# Codex provider notes

Codex likely needs:
- account snapshot/switching support
- OAuth/session credential discovery
- optional usage/quota probe support
- safe separation between auth logic and usage UI
